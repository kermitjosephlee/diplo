// checks to see if convoy is broken by attack and insufficient support
// returns updated convoyObj = { valid, invalid }
function convoyInterruptionChecker(convoys, movements) {
	const { validConvoys, invalidConvoys } = convoys;

	// console.log(
	// 	"validConvoys",
	// 	validConvoys,
	// 	"\ninvalidConvoys",
	// 	invalidConvoys,
	// 	"\nmovements",
	// 	movements
	// );
	return convoys;
}

exports.convoyInterruptionChecker = convoyInterruptionChecker;
